-   
    const statusMap = {
        "Pending" : {
            icon : "clock",
            color : "warning"
            },
        "Completed" : {
            icon : "check-cicle",
            color : "success"
        },
        "Missed" : {
            icon : "x-circle",
            color : "danger"
        }
    }


- 
    function formatAMPM(date) {
    var hours = date.getHours();
    var minutes = date.getMinutes();
    var ampm = hours >= 12 ? 'pm' : 'am';
    hours = hours % 12;
    hours = hours ? hours : 12; // the hour '0' should be '12'
    minutes = minutes < 10 ? '0'+minutes : minutes;
    var strTime = `${hours}:${minutes}${ampm} ${date.getDate()}/${date.getMonth()+1}/${date.getFullYear()}`;
    return strTime;
    }

.container.border.bg-light.mt-4.p-4
    .d-flex
        p.h3= subtask.title
    .d-flex.justify-content-between.gap-4
        p.d-flex.align-center.gap-1
            i.w4.h4(class = `ph-${statusMap[subtask.status].icon} text-${statusMap[subtask.status].color}`)
            span.fw-bold= subtask.status
        p
            b Deadline : 
            = formatAMPM(subtask.deadline)
    .container
        p.fs-4 #[span.fs-2= subtask.description[0]]#{subtask.description.slice(1)}
    .d-flex.justify-content-end
        p
            b Assignee :
            = ` ${subtask.asignee}`
    hr.mt-0
    .d-flex
        p.fw-bold File : #[a(href="#") #{subtask.slug} &darr;]
    .d-flex.gap-2
        if (user.role==='lead' || user.role==='coordinator')
            button.btn.btn-secondary.col-6 Edit
            button.btn.btn-secondary.col-6 Delete
    .d-flex.gap-2.mt-2
        button.btn.btn-secondary.col-6(onclick=`assign("${subtask.id}")`) Assign me
        button.btn.btn-secondary.atc-button.col-6 Add To Calendar
